apiVersion: v1
kind: Service
metadata:
    name: arnedeproftazurevotingappredis
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: arnedeproftazurevotingappredis